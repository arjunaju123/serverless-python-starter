frameworkVersion: '3'
<<<<<<< Updated upstream
=======

>>>>>>> Stashed changes
service: serverless-stack-python-starter

plugins:
  - serverless-python-requirements

custom:
  pythonRequirements:
    dockerizePip: false

provider:
  name: aws
  runtime: python3.9
  stage: dev
  region: us-east-1
  memorySize: 128
  timeout: 10

package:
  patterns:
    - '!node_modules/**'
    - '!test/**'
    - '!tests/**'
    - '!*.md'
    - '!*.log'

package:
  patterns:
    - '!node_modules/**'
    - '!test/**'
    - '!tests/**'
    - '!__pycache__/**'
    - '!*.md'
    - '!*.tmp'

functions:
  hello:
    handler: handler.hello
    memorySize: 128
    timeout: 10
    events:
      - httpApi:
          path: /
          method: get